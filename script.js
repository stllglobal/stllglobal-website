const btn=document.querySelector('.menu');const nav=document.querySelector('.nav nav');btn.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));